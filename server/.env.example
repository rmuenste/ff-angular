# Backend Server Environment Configuration Template
#
# SETUP INSTRUCTIONS:
# This project follows the convention of using environment-specific configuration files:
#   - .env.development   (for local development)
#   - .env.staging       (for staging environment) 
#   - .env.production    (for production environment)
#   - .env               (optional: local overrides)
#
# DEVELOPER RESPONSIBILITY:
# 1. Copy this file to create your environment-specific configs:
#    cp .env.example .env.development
#    cp .env.example .env.staging  
#    cp .env.example .env.production
#
# 2. Update each file with appropriate values for that environment
#
# 3. NEVER commit the actual .env.* files (except .env.example)
#
# 4. The server automatically loads the correct file based on ENVIRONMENT variable:
#    ENVIRONMENT=development python server.py  # loads .env.development
#    ENVIRONMENT=staging python server.py      # loads .env.staging
#    ENVIRONMENT=production python server.py   # loads .env.production

# Server Configuration
PORT=8000
HOST=127.0.0.1

# CORS Configuration (comma-separated list)
ALLOWED_ORIGINS=http://localhost:4200

# Data Directory
DATA_DIRECTORY=data

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Security Settings
ENABLE_RATE_LIMITING=true
RATE_LIMIT_REQUESTS=1000
RATE_LIMIT_WINDOW=3600